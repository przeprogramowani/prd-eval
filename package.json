{
  "name": "prd-eval",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "eval": "npm run build && PROMPTFOO_CACHE_ENABLED=false node dist/evalRunner.js",
    "eval:watch": "tsc --project tsconfig.json --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "display-scores": "npm run build && node dist/resultInterpreter.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.16.5",
    "promptfoo": "^0.118.11",
    "ts-node": "^10.9.2",
    "tsx": "^4.7.1",
    "typescript": "^5.4.5"
  }
}
